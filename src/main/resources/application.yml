app:
  jwt:
    key: abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ1234567890
    exp: 18000000

server:
   port: 8080

eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: https://${eureka.instance.hostname}:${{ CINEMA_DISCOVERY_SERVER }}/eureka

spring:
   application:
     name: auth-service
   jpa:
     hibernate:
       ddl-auto: update
   datasource:
     url: jdbc:postgresql://localhost:5432/bioskop_db
     # jdbc:postgresql://containers-us-west-68.railway.app:6368/railway
     # password : xhtTI10rPJkQvUXUpfVz
     username: postgres
     password: password
     driver-class-name: org.postgresql.Driver
